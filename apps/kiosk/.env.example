# Chỉ cần set khi kiosk deploy tách domain với backend (VD: Cloudflare Pages).
# Để trống hoặc không tạo file .env -> app tự dùng đường dẫn tương đối,
# chạy đúng như cũ với `npm run dev` (proxy tới localhost:8787) hoặc khi
# server/ tự serve luôn bản build kiosk.
VITE_API_BASE=
